# Bittensor
BT_NETUID=103
BT_NETWORK=finney
BT_WALLET_NAME=default
BT_WALLET_HOTKEY=default

# Miner API
API_HOST=0.0.0.0
API_PORT=8422

# CORS origins (comma-separated, leave empty for wildcard in dev)
# CORS_ORIGINS=https://app.djinn.com,https://staging.djinn.com

# The Odds API — get a key at https://the-odds-api.com
ODDS_API_KEY=your-odds-api-key-here
ODDS_API_BASE_URL=https://api.the-odds-api.com

# Cache TTL in seconds for odds data (0 = no caching)
ODDS_CACHE_TTL=30

# Line matching tolerance (±0.5 means a line of -3.0 matches -2.5 to -3.5)
LINE_TOLERANCE=0.5

# TLSNotary
# TLSN_NOTARY_HOST=notary.pse.dev     # PSE public notary (default)
# TLSN_NOTARY_PORT=443                # Use 7047 for local notary server
# TLSN_PROVER_BINARY=djinn-tlsn-prover

# Timeouts (seconds) — defaults shown
HTTP_TIMEOUT=30

# Logging — defaults shown
LOG_FORMAT=console
LOG_LEVEL=INFO
