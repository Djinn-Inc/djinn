{
  "name": "circuits",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "compile": "circom src/audit_proof.circom --r1cs --wasm --sym -o build/ && circom src/track_record.circom --r1cs --wasm --sym -o build/",
    "setup": "bash scripts/setup.sh",
    "test": "node test/audit_proof.test.js && node test/track_record.test.js",
    "test:all": "node test/audit_proof.test.js && node test/track_record.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.29.3",
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.6"
  },
  "devDependencies": {
    "circomlibjs": "^0.1.7"
  }
}
